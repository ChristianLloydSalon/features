import 'package:flutter/material.dart';

class {{feature.pascalCase()}}Screen extends StatelessWidget {
  const {{feature.pascalCase()}}Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
